# trigger-zeit-serverless

This is a demo of serverless deployments of a remote schema and an event trigger on Zeit Now 2.0 Platform to build a 3factor app.

- Remote schema is used to fetch weather information
- Event trigger is used to reverse the name of a profile